# Deployment Notes

Deployment environment prepared using:

- Docker Desktop
- WSL2
- FastAPI
- PostgreSQL

Deployment files:

- Dockerfile
- docker-compose.yml

Status

Docker Desktop and WSL2 were successfully configured.

Metabase image deployment was attempted but could not be completed due to an external Docker image download (EOF) error.